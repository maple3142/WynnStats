language: node_js
node_js:
  - "7"
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  local_dir: dist
  target_branch: master
  project_name: WynnStats
  repo: WynnStats/WynnStats.github.io
  on:
    branch: master
